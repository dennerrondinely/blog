---
title: 'Desafio de Entrevista: Inverter Strings com Regras Específicas em JavaScript'
description: 'Aprenda a resolver um desafio de entrevista técnica: inverter strings mantendo a ordem das palavras e preservando caracteres especiais nas suas posições originais.'
date: '2026-02-14'
tags: ['javascript', 'algorithms', 'interviews', 'problem-solving', 'strings']
locale: 'pt'
coverImage: '/blog/capas/reverse-string-interview.jpg'
---

# 💡 Desafio de Entrevista: Inverter Strings com Regras Específicas

Recentemente participei de uma entrevista técnica onde recebi um desafio aparentemente simples, mas com regras bem específicas: **inverter uma string em JavaScript**.

O desafio parecia fácil no começo, mas had detalhes importantes que tornavam bem mais interessante. Vou compartilhar minha solução e explicar o raciocínio por trás.

---

## 🎯 O Desafio

Criar uma função que:

1. ✅ **Inverta cada palavra** da string individualmente
2. ✅ **Mantenha a ordem** das palavras na frase
3. ✅ **Preserve caracteres especiais** nas suas posições originais (não invertendo-os)

### Exemplos

```javascript
// Exemplo 1: Palavra simples
reverseString("hello") 
// Esperado: "olleh"

// Exemplo 2: Múltiplas palavras
reverseString("hello world")
// Esperado: "olleh dlrow"

// Exemplo 3: Com caracteres especiais (não se movem!)
reverseString("he-llo")
// Esperado: "ol-leh"

// Exemplo 4: Múltiplas palavras com caracteres especiais
reverseString("hello-world test")
// Esperado: "olleh-dlrow tset"

// Exemplo 5: Caracteres especiais mantêm posição
reverseString("a-b-c")
// Esperado: "c-b-a"
```

**O ponto-chave:** Os caracteres especiais (`-`, `!`, `.`, etc.) não mudam de posição. Apenas as letras são invertidas ao redor deles.

---

## 🤔 Análise do Problema

Vamos entender melhor com o exemplo `"he-llo"`:

1. Caracteres: `h`, `e`, `-`, `l`, `l`, `o`
2. Posições dos caracteres especiais: `-` está na posição 2
3. Caracteres normais (excluindo `-`): `h`, `e`, `l`, `l`, `o`
4. Invertidos: `o`, `l`, `l`, `e`, `h`
5. Reinserir `-` na posição 2: `o`, `l`, `-`, `l`, `e`, `h` → `"ol-leh"`

**Estratégia:**
1. Dividir a string em palavras
2. Para cada palavra:
   - Extrair apenas caracteres alfabéticos
   - Inverter esses caracteres
   - Reinserir caracteres especiais nas posições originais
3. Unir as palavras de volta

---

## 💻 Minha Solução

### Versão 1: Simples e Legível

```javascript
function reverseString(str) {
  return str
    .split(' ')                           // Dividir por espaços
    .map(word => reverseWord(word))       // Inverter cada palavra
    .join(' ');                           // Reunir com espaços
}

function reverseWord(word) {
  // Extrair apenas caracteres alfabéticos
  const letters = word.split('').filter(char => /[a-zA-Z]/.test(char));
  
  // Inverter os caracteres alfabéticos
  letters.reverse();
  
  // Reconstruir a palavra
  let letterIndex = 0;
  return word
    .split('')
    .map(char => {
      if (/[a-zA-Z]/.test(char)) {
        return letters[letterIndex++];
      }
      return char; // Caractere especial mantém sua posição
    })
    .join('');
}

// Teste
console.log(reverseString("hello world"));        // "olleh dlrow"
console.log(reverseString("he-llo"));             // "ol-leh"
console.log(reverseString("hello-world test"));   // "olleh-dlrow tset"
```

**Como funciona:**
1. `reverseString()` divide a string por espaços e processa cada palavra
2. `reverseWord()` extrai letras, as inverte, e reconstrói mantendo especiais no lugar
3. Usa regex `/[a-zA-Z]/` para identificar caracteres alfabéticos

---

### Versão 2: Mais Compacta

```javascript
function reverseString(str) {
  return str
    .split(' ')
    .map(word => {
      const letters = word.split('').filter(c => /[a-zA-Z]/).reverse();
      let i = 0;
      return word.split('').map(c => /[a-zA-Z]/.test(c) ? letters[i++] : c).join('');
    })
    .join(' ');
}
```

---

### Versão 3: Com Suporte a Caracteres Especiais Customizados

Se o entrevistador quisesse especificar quais caracteres invertir:

```javascript
function reverseString(str, preserveChars = /[^a-zA-Z]/) {
  return str
    .split(' ')
    .map(word => reverseWord(word, preserveChars))
    .join(' ');
}

function reverseWord(word, preserveChars) {
  // Extrair apenas caracteres que NÃO são preservados
  const chars = word.split('').filter(char => !preserveChars.test(char));
  chars.reverse();
  
  // Reconstruir
  let charIndex = 0;
  return word
    .split('')
    .map(char => 
      preserveChars.test(char) ? char : chars[charIndex++]
    )
    .join('');
}

// Uso
console.log(reverseString("hello-world")); // "olleh-dlrow"
// Customizar: apenas manter números
console.log(reverseString("hel3lo w0rld", /[0-9]/)); // "0lrw dl3leh"
```

---

## 🧪 Casos de Teste

```javascript
// Casos básicos
console.assert(reverseString("hello") === "olleh", "Palavra simples");
console.assert(reverseString("hello world") === "olleh dlrow", "Múltiplas palavras");

// Com caracteres especiais
console.assert(reverseString("he-llo") === "ol-leh", "Hífen no meio");
console.assert(reverseString("hello!") === "olleh!", "Pontuação no final");
console.assert(reverseString("h-e-l-l-o") === "o-l-l-e-h", "Múltiplos hífens");

// Casos complexos
console.assert(
  reverseString("hello-world test!") === "olleh-dlrow tset!",
  "Múltiplas palavras com especiais"
);

// Vazio e casos extremos
console.assert(reverseString("") === "", "String vazia");
console.assert(reverseString("a") === "a", "Caractere único");
console.assert(reverseString("!!!") === "!!!", "Apenas especiais");
console.assert(reverseString("-a-b-c-") === "-c-b-a-", "Especiais nas extremidades");

console.log("✅ Todos os testes passaram!");
```

---

## 📊 Análise de Complexidade

| Aspecto | Complexidade | Explicação |
|---------|--------------|-----------|
| **Tempo** | O(n) | Percorre a string uma vez |
| **Espaço** | O(n) | Cria arrays temporários |

Onde `n` é o comprimento da string.

---

## 🎓 O que o Entrevistador Estava Procurando

Na entrevista, perceba que o entrevistador queria avaliar:

### 1. **Entendimento do Problema**
- Clarificar requisitos (o que inverte? o que não inverte?)
- Fazer perguntas (espaços múltiplos? tabs? acentos?)

### 2. **Raciocínio Algorítmico**
- Dividir problema em partes menores (palavra por palavra)
- Lidar com detalhes (caracteres especiais)

### 3. **Implementação Limpa**
- Código legível com nomes claros
- Usar funções auxiliares
- Evitar soluções super complexas

### 4. **Testes**
- Pensar em edge cases
- Validar a solução

---

## 💬 Durante a Entrevista

**Dicas para quando receber esse tipo de desafio:**

### ✅ O que fazer
- 🗣️ **Falar em voz alta** - Explique seu raciocínio
- 🤔 **Pergunte** - "Posso assumir que a entrada é uma string válida?"
- 🧪 **Teste casos extremos** - String vazia, caracteres especiais, etc
- ♻️ **Refatore** - Depois de funcionar, melhore o código
- 📝 **Explique** - Mostre que entendeu a solução

### ❌ O que evitar
- 🏃 **Não se apresse** - Claridade é mais importante que velocidade
- 🤐 **Não fique em silêncio** - Mostre seu pensamento
- 🔄 **Não refatore indefinidamente** - Pare quando ficar bom
- ❌ **Não ignore edge cases** - Eles importam

---

## 🚀 Versão com RegEx (Avançada)

Se quisesse impressionar mais:

```javascript
function reverseString(str) {
  return str.replace(/\S+/g, word => {
    const letters = word.match(/[a-zA-Z]/g) || [];
    let i = letters.length;
    
    return word.replace(/[a-zA-Z]/g, () => letters[--i]);
  });
}

console.log(reverseString("hello-world test!"));
// "olleh-dlrow tset!"
```

**Como funciona:**
- `/\S+/g` - Encontra todas as palavras (sem espaços)
- `match(/[a-zA-Z]/g)` - Extrai apenas letras
- `replace(/[a-zA-Z]/g, ...)` - Substitui letras mantendo especiais

---

## 🎯 Conclusão

Este desafio parece simples, mas tem várias lições:

1. **Requisitos claros** - Pergunte antes de codificar
2. **Dividir problemas** - Quebre em partes menores
3. **Detalhes importam** - Caracteres especiais não são triviais
4. **Testes são essenciais** - Valide sua solução

A solução final é elegante, eficiente e mostra bom raciocínio algorítmico. Isso é exatamente o que os entrevistadores procuram!

---

## 🔗 Recursos

- [MDN: String Methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String)
- [MDN: Regular Expressions](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions)
- [LeetCode - Reverse String](https://leetcode.com/problems/reverse-string/)